import random
random.seed()

def tri():
    random.seed()
    x = random.random()
    y = random.random()
    z = random.random()
    cord = [x,y,z]
    return cord
#genera tripletta numeri random float tra 0 e 1
k=10
cord =[[1,2,3]]*k
d1=[item[0] for item in cord] 
print(d1)
#while success==false

for i in range(k):
    #cord[i] = [random.random() , random.random() , random.random()]
    appoggio = tri()
    cord[i] = appoggio


print(len(cord))
print(cord)
